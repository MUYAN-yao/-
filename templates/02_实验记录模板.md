# 实验记录与敏感性方案模板

请复制本模板到自己的工作目录后填写，不修改提供的原件。每次运行使用独立记录；失败和中断也要留档。

## A. 基本信息

- 记录 ID / 日期：
- 执行者：
- 本次状态：原实现重放 / 仅设计、未运行 / 已获准的追加分析 / 失败 / 中断
- 本次问题（一句话）：
- 导师确认记录（原实现重放之外的分析必填；未确认写“未确认，不执行”）：
- 输入包版本 / 校验清单：
- Python / NumPy / 操作系统版本：
- 代码副本路径与哈希或版本：
- 本次输出目录（必须是新目录）：
- 命令原文与工作目录：
- AI 工具及具体用途（不得粘贴密钥或敏感数据）：

## B. 输入与范围核对

| 项目 | 预期 | 实测 / 证据文件 | 是否一致 |
|---|---|---|---|
| 测试视频 | 150 |  |  |
| mixed-label 审计目标 | 134 |  |  |
| 全正常 / 全异常视频 | 10 / 6 |  |  |
| 每个目标 donor | 8，均不等于目标 |  |  |
| 候选 / 哨兵数 | 3 / 4，全部报告 |  |  |
| 对齐规则 | 候选原始分数 `[1:]` 与标签严格等长 |  |  |
| bootstrap | 视频单位；10000 次；种子 24082026 |  |  |
| CNM 哨兵选择 | 每次抽样重新取四哨兵最大值 |  |  |
| 输入是否改动 | 否 |  |  |
| 是否新增训练或推理 | 否 |  |  |

如果计数不一致，先定位输入、对齐或代码版本问题；不要删样本以达到预期数。150/134 是本包预期，不是可用筛选强行达成的目标。

## C. 原实现重放结果

保留完整 JSON 等机器可读结果，下面表格仅供阅读。参考与重算列可分别填写为“参考 → 重算”；不要只比较四舍五入的论文数值。

| 候选 | TIA 点估计 | TIA 95% 区间 | TIA 同时单侧下界 | CNM 点估计 | CNM 95% 区间 | CNM 同时单侧下界 |
|---|---|---|---|---|---|---|
| 正常特征距离 |  |  |  |  |  |  |
| 时序新颖度 |  |  |  |  |  |  |
| 相邻帧差分 |  |  |  |  |  |  |

| 候选 | 普通 frame AUC | 普通 frame AP | AP−异常帧比例 | CIL 各项检查 / 判定 | 普通效用检查 / 总判定 |
|---|---|---|---|---|---|
| 正常特征距离 |  |  |  |  |  |
| 时序新颖度 |  |  |  |  |  |
| 相邻帧差分 |  |  |  |  |  |

| 哨兵 | TIA 点估计 | 95% 区间 | 参考结果差异 |
|---|---|---|---|
| `sentinel_center_prior` |  |  |  |
| `sentinel_duration_center` |  |  |  |
| `sentinel_linear_progress` |  |  |  |
| `sentinel_sampling_grid` |  |  |  |

- 五个分组的结果 / 所有布尔检查保存位置：
- 完整逐视频效应保存位置：
- 字段比较方法、容差及理由：
- 输入/代码哈希是否一致：
- 注意：文件路径、运行环境或记录元信息的变化与科学数值差异应分开报告；不得删去差异字段来声称一致。

## D. 差异与异常记录

| 时间 / 位置 | 预期 | 实际观察 | 已检查的解释 | 尚不能排除的解释 | 下一步 / 是否需导师决定 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

包括失败运行、输入缺失、长度冲突、数值差异和文档歧义。没有发现差异时，写“在所列字段、输入版本和容差范围内未发现”，不要写“证明实现绝对正确”。

## E. 第二阶段方案（默认未确认、未运行）

- 方案状态：未确认、未运行 / 导师要求修订 / 已确认（附日期和范围）
- 科学问题：哨兵生成网格的两种约定是否改变现有审计结论？
- 假设与可反驳条件：
- 为什么该比较必要；也有哪些理由支持不追加实验：

唯一拟变因素：对每个长度为 N 的标签序列，A 为 `sentinels(N)`；B 为 `sentinels(N+1)` 的每条输出曲线再取 `[1:]`。目标和 donor 两侧采用相同条件。两者均保留原候选 `score[1:]`、标签、秩变换和进度插值不变。

固定项请逐一确认：

- 150 视频 donor 池与 134 mixed-label 目标不变；8 个 donor 映射不变。
- 3 候选、4 哨兵全部报告，不追选子集。
- bootstrap 种子 24082026、10000 次、视频单位不变；拟复用相同抽样索引作配对比较。
- 每个条件中，每次抽样都重新计算最大哨兵；同时推断、五组检查和既有阈值不变。
- 不改变普通检测指标的候选分数；如其数值变化，应先查是否误改了不变量。
- 新结果另存；原始确认结果及不利条件均保留。

计划表（在运行追加条件之前填写）：

| 输出 | 计划如何比较 | 什么观察会改变主张 | 如何完整报告 |
|---|---|---|---|
| 三候选 TIA |  |  |  |
| 四哨兵 TIA |  |  |  |
| 三候选 CNM 与区间 |  |  |  |
| 逐视频效应 / 异常 |  |  |  |
| 各检查和最终判定 |  |  |  |

- 停止条件 / 最大执行范围（例如只比较 A 与 B，不扩展更多网格）：
- 对“差异很小”的描述标准及理由（不能看完结果才选择一个有利标准）：
- 如果原结论不稳健，拟收窄的具体措辞：
- 如果原结论稳健，仍不能声称什么：
- 导师决定：不做追加 / 先核对协议来源 / 修改方案 / 同意在上述范围运行 / 直接收窄结论

这里是事后敏感性分析的设计，不是新的盲评或独立确认。旧封存记录中的“标签开启”等历史字段描述原实验，不代表学生获得了一份新的未见标签测试集。

## F. 结论与交接

- 已观察到的事实：
- 由事实支持的最窄结论：
- 不能从中推出的结论：
- 尚未解决的问题：
- 能直接用于论文的文字草稿（注明重放或敏感性分析身份）：
- 完整输出、环境信息、日志与图表所在位置：
- 本次是否需要导师行动；如需要，具体决定是什么：
